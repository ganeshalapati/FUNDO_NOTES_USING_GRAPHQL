//write a program to add all the whole squares from 1 to 1000 using arrays 
function sum(array){
    const sum = 0;
    i = array.length;
    while(i--)
    sum = Math.pow(array[i],2);
    return sum
}

console.log(sum[]);